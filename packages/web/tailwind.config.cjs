const config = {
	mode: 'jit',
	purge: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		// fontFamily: {
		// 	sans: ['Cabin', 'sans-serif'],
    //   serif: ['Libre Baskerville', 'serif'],
		// },
		extend: {}
	},

	plugins: []
};

module.exports = config;
